local {ClassName} = BaseClass("{ClassName}",BindingModel);

---初始化数据
function {ClassName}:Initlialize()
	--例如self.字段名 = BindableProperty(字段名);
end

---释放数据
function {ClassName}:Dispose()
end

return {ClassName}
